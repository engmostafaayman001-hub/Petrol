import React, { FormEvent, useCallback, useEffect, useState } from 'react';
import PageLayout from '../src/components/PageLayout';
import supabase from '../src/lib/supabaseClient';
import { useRequireAuth } from '../src/lib/auth';
import { EmptyState, ErrorState, LoadingState } from '../src/components/DataState';
type Profile = { id: string; full_name: string; email: string; role: 'manager'|'supervisor'; is_active: boolean; last_seen_at?: string | null };
const blank = { full_name: '', email: '', password: '', role: 'supervisor' as Profile['role'], is_active: true };
export default function UsersPage(){const {user}=useRequireAuth();const[rows,setRows]=useState<Profile[]>([]);const[state,setState]=useState<'loading'|'ready'|'error'>('loading');const[form,setForm]=useState(blank);const[editing,setEditing]=useState<Profile|null>(null);const[message,setMessage]=useState('');const token=async()=>{const {data}=await supabase.auth.getSession();return data.session?.access_token||''};const load=useCallback(async()=>{setState('loading');try{const r=await fetch('/api/users',{headers:{Authorization:`Bearer ${await token()}`}});const d=await r.json();if(!r.ok)throw Error(d.error);setRows(d.users);setState('ready')}catch{setState('error')}},[]);useEffect(()=>{if(user)load()},[user,load]);async function submit(e:FormEvent){e.preventDefault();setMessage('');const method=editing?'PATCH':'POST';const payload=editing?{...form,id:editing.id,email:undefined}:{...form};const r=await fetch('/api/users',{method,headers:{'Content-Type':'application/json',Authorization:`Bearer ${await token()}`},body:JSON.stringify(payload)});const d=await r.json();if(!r.ok){setMessage(d.error||'تعذر حفظ المستخدم.');return}setForm(blank);setEditing(null);setMessage(editing?'تم تحديث المستخدم.':'تم إنشاء المستخدم.');load()}async function remove(profile:Profile){if(!window.confirm(`هل تريد حذف حساب ${profile.full_name} نهائيًا؟`))return;const r=await fetch('/api/users',{method:'DELETE',headers:{'Content-Type':'application/json',Authorization:`Bearer ${await token()}`},body:JSON.stringify({id:profile.id})});const d=await r.json();if(!r.ok){setMessage(d.error||'تعذر حذف المستخدم.');return}setMessage('تم حذف المستخدم.');load()}function edit(profile:Profile){setEditing(profile);setForm({full_name:profile.full_name,email:profile.email,password:'',role:profile.role,is_active:profile.is_active});window.scrollTo({top:0,behavior:'smooth'})}return <PageLayout title="المستخدمون والصلاحيات"><div className="page-heading"><div><h2>المستخدمون والصلاحيات</h2><p>أضف الفريق وحدد صلاحيات كل حساب بأمان.</p></div><span className="status-badge">{rows.filter(r=>r.is_active).length} مستخدم نشط</span></div><div className="grid gap-5 xl:grid-cols-[390px_1fr]"><form onSubmit={submit} className="ui-card form-card form-grid"><h3 className="m-0 text-lg font-extrabold">{editing?'تعديل المستخدم':'إضافة مستخدم'}</h3><div className="form-field"><label>الاسم الكامل</label><input required value={form.full_name} onChange={e=>setForm({...form,full_name:e.target.value})}/></div><div className="form-field"><label>البريد الإلكتروني</label><input required disabled={!!editing} type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="form-field"><label>{editing?'كلمة مرور جديدة (اختياري)':'كلمة المرور'}</label><input required={!editing} minLength={6} type="password" value={form.password} onChange={e=>setForm({...form,password:e.target.value})}/></div><div className="form-field"><label>الدور</label><select value={form.role} onChange={e=>setForm({...form,role:e.target.value as Profile['role']})}><option value="supervisor">مشرف</option><option value="manager">مدير</option></select></div><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.is_active} onChange={e=>setForm({...form,is_active:e.target.checked})}/> حساب نشط</label>{message&&<p className="text-sm text-[var(--text-muted)]">{message}</p>}<div className="flex gap-2"><button className="ui-button">{editing?'حفظ التعديل':'إضافة المستخدم'}</button>{editing&&<button type="button" className="ui-button secondary" onClick={()=>{setEditing(null);setForm(blank)}}>إلغاء</button>}</div></form><section className="ui-card"><div className="ui-toolbar"><b>قائمة الفريق</b><span className="text-xs text-[var(--text-muted)]">المدير فقط يستطيع الإدارة</span></div>{state==='loading'?<LoadingState/>:state==='error'?<ErrorState onRetry={load}/>:rows.length===0?<EmptyState title="لا يوجد مستخدمون" description="أضف أول مستخدم للفريق من النموذج."/>:<div className="table-scroll"><table className="data-table"><thead><tr><th>المستخدم</th><th>الدور</th><th>الحالة</th><th>آخر نشاط</th><th>الإجراءات</th></tr></thead><tbody>{rows.map(profile=><tr key={profile.id}><td><b>{profile.full_name}</b><small className="block mt-1 text-[var(--text-muted)]">{profile.email}</small></td><td><span className="status-badge">{profile.role==='manager'?'مدير':'مشرف'}</span></td><td>{profile.is_active?'نشط':'موقوف'}</td><td>{profile.last_seen_at?new Intl.DateTimeFormat('ar-EG',{dateStyle:'medium'}).format(new Date(profile.last_seen_at)):'—'}</td><td><div className="flex gap-2"><button className="ui-button secondary !min-h-[32px] !px-3" onClick={()=>edit(profile)}>تعديل</button><button className="ui-button danger !min-h-[32px] !px-3" disabled={profile.id===user?.id||profile.email.toLowerCase()==='markode@gmail.com'} onClick={()=>remove(profile)}>حذف</button></div></td></tr>)}</tbody></table></div>}</section></div></PageLayout>}
